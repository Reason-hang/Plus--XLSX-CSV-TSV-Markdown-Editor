# 测试与验收清单

> 状态：静态与编译验证已通过；Extension Host 在当前 macOS 执行环境启动时 SIGABRT，真实 IDE 验收按目标环境执行
> 更新时间：2026-09-15
> 文档类型：测试长文档

## 目录

- [验收原则](#验收原则)
- [自动化检查](#自动化检查)
- [人工验收矩阵](#人工验收矩阵)
- [失败处理](#失败处理)
- [上线门槛](#上线门槛)

## 验收原则

自动化构建只能证明代码、配置和产物满足静态约束，不能代替真实 VS Code、Cursor、Antigravity 的安装和交互验证。主题系统与表格功能分别验收，避免“主题通过”被误认为“所有编辑器功能通过”。

## 自动化检查

在仓库根目录执行：

| 命令 | 验证内容 | 通过标准 |
| --- | --- | --- |
| npm run theme:build | Less 编译、CSS 作用域、manifest | 返回 0，生成 CSS 和 manifest |
| npm run theme:audit | MPE 样式迁移扫描 | 生成报告和迁移草稿 |
| npm run check-types | TypeScript 类型 | 返回 0 |
| npm run lint | ESLint | 不能有 error；warning 必须注明来源 |
| npm run verify:local-patch | mark 标签、标题颜色设置、工具栏总高度布局、深色交替行和运行时标记 | 返回 0 |
| npm run verify:security | HTML/CSP、消息 schema、payload/坐标/realpath/KaTeX/StyleStorage 回归 | 返回 0 |
| npm run verify:theme-system | 外置主题配置、命令、产物和注入逻辑 | 返回 0 |
| npm run verify:docs | 文档索引、内部链接和跨平台路径 | 返回 0 |
| npm test | Extension Host 安全/可靠性回归（首次运行会下载 VS Code） | 测试文件编译成功并在可用 Extension Host 中全部通过 |
| npm run package | 生产构建 | 返回 0，生成 dist/ 产物 |
| @vscode/vsce package | VSIX 组装 | 包内无主题工程 node_modules |
| npm audit --audit-level=high --package-lock-only | npm 锁文件漏洞 | `.10` 当前结果为 0 项（0 low、0 moderate、0 high、0 critical） |

当前静态检查没有 lint error；Lint 仅保留 `src/spreadsheet/spreadsheetUtilities.ts` 原有 2 个 curly warning。`.10` 审计发现的问题已修复并复核，详见 [`.10` 代码审计报告](./代码审计报告-2026-09-15-v1.9.98-local.10.md)。历史 `.8`/`.9` 审计和交付证据见 [历史审计报告](./代码审计报告-2026-09-15-v1.9.98-local.7.md)，不替代 `.10` 当前事实。

`npm test` 的测试源编译和 CLI 配置已通过；本机 VS Code 1.137.0 Extension Host 以 `SIGABRT` 终止。Docker 本轮 Node 24 arm64 干净安装 615 个包且审计为 0 项，但 Linux VS Code 运行时下载未进入测试进程，因此本轮不把 Docker Extension Host 记为通过；三 IDE 现场验收仍需单独完成。

## 人工验收矩阵

| 场景 | VS Code | Cursor | Antigravity |
| --- | --- | --- | --- |
| 安装本地 VSIX | 待现场验证 | 待现场验证 | 待现场验证 |
| 打开 Markdown 并左右分栏 | 待现场验证 | 待现场验证 | 待现场验证 |
| mark 标签橙底 | 待现场验证 | 待现场验证 | 待现场验证 |
| h1-h6 默认蓝色与标题颜色配置 | 待现场验证 | 待现场验证 | 待现场验证 |
| Split Edit/Preview Edit 格式工具栏完整显示、所有按钮可点击、内容区不覆盖 | 待现场验证 | 待现场验证 | 待现场验证 |
| KaTeX 断网公式渲染 | 待现场验证 | 待现场验证 | 待现场验证 |
| 外部图片根目录与 symlink 边界 | 待现场验证 | 待现场验证 | 待现场验证 |
| 双栏字号与行高设置 | 待现场验证 | 待现场验证 | 待现场验证 |
| Settings 面板 Markdown appearance 配置持久化 | 待现场验证 | 待现场验证 | 待现场验证 |
| 内置重点高亮快捷键 `⌘⌥⇧3` | 待现场验证 | 待现场验证 | 待现场验证 |
| 修改 CSS 后自动刷新 | 待现场验证 | 待现场验证 | 待现场验证 |
| 主题 CSS 错误时保留旧样式 | 待现场验证 | 待现场验证 | 待现场验证 |
| XLSX 打开、编辑、保存 | 待现场验证 | 待现场验证 | 待现场验证 |
| CSV、TSV 打开、编辑、保存 | 待现场验证 | 待现场验证 | 待现场验证 |

人工验收步骤：

1. 安装 VSIX 并重载窗口。
2. 配置 xlsxViewer.md.theme.enabled、cssFile、manifestFile 和 watch。
3. 打开 Markdown，确认编辑、预览、目录、保存和 mark 高亮。
4. 修改 theme.less，运行 npm run theme:build，确认 CSS 文件变化并刷新预览。
5. 临时制造 CSS 语法或路径错误，确认预览不白屏且状态可定位。
6. 分别打开并保存 XLSX、CSV、TSV，确认原功能无回归。
7. 将快捷键冲突、字号配置和窗口重载结果记录到对应版本记录表，不能只写“已验证”。
8. 在 `Settings` 面板分别修改高亮、标题颜色、预览和编辑字号/行高，确认当前 Webview 立即更新；重载窗口后再次打开面板，确认值仍保持且空的预览字段继续跟随 IDE 主题。
9. 缩窄 `Split Edit` 与 `Preview Edit`，确认格式工具栏自动换行、所有工具组可见且可点击，内容区不覆盖格式栏；检查深色表格奇数行为 `#161617`、偶数行为 `#27272A`。
10. 断开网络后打开包含公式的 Markdown，确认 KaTeX 仍能渲染；对工作区外图片只配置明确可信根，确认未授权 symlink 不显示。
11. 记录超限消息、超大编辑批次、非法坐标、超大合并和 StyleStorage 超限的拒绝结果，确认原文件和上一份有效元数据未被破坏。

安全专项至少补测：Markdown 中的事件属性、脚本、`<style>`、不安全链接和 Mermaid 代码；表格单元格中的 HTML、属性引号、`0`、`false`、富文本；在外部程序修改文件后再保存，确认扩展拒绝静默覆盖；模拟转换后样式应用失败，确认目标文件仍被正确报告为已转换。

## 失败处理

- 配置错误：先执行“显示外置 Markdown 主题状态”，核对绝对路径。
- manifest 不匹配：重新运行 npm run theme:build，不要手改 manifest。
- 主题选择器无效：检查目标 Webview 的 DOM class，再更新主题 partial。
- 主题编译失败：修复 Less，运行时会继续使用上一次成功 CSS；不要删除旧产物。
- 表格功能异常：关闭外置主题后复测，区分主题问题和上游功能问题。

## 上线门槛

本地侧载交付至少满足：

- 自动化检查全部通过。
- VSIX 可以安装并能回退。
- 至少一套真实 IDE 完成 Markdown 主流程。
- XLSX、CSV、TSV 各完成一次打开、编辑、保存回归。
- 主题路径、构建失败、运行时回退和升级步骤有记录。

Marketplace 或 Open VSX 发布还需要额外完成 Publisher、扩展身份、许可证、隐私声明、商店元数据和多平台发布审核，本清单不把它们默认为已完成。
