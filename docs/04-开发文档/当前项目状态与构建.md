# 当前项目状态与构建

> 状态：当前版本
> 更新时间：2026-08-31
> 文档类型：开发长文档

## 目录

- [当前基线](#当前基线)
- [目录结构](#目录结构)
- [安装依赖](#安装依赖)
- [构建与打包](#构建与打包)
- [开发约束](#开发约束)
- [升级流程](#升级流程)

## 当前基线

| 项目 | 当前值 |
| --- | --- |
| 上游基线 | muhammad-ahmad.xlsx-viewer v1.9.97 |
| 本地补丁版本 | 1.9.98-local.3 |
| 扩展标识 | muhammad-ahmad.xlsx-viewer |
| 发布方式 | 本地构建 VSIX，未发布到 Marketplace 或 Open VSX |
| 主题源 | themes/markdown-theme/theme.less |
| 主题产物 | themes/markdown-theme/dist/markdown-theme.css |
| 在线依赖审计 | 8 项（4 low、3 moderate、1 high、0 critical）；运行时公式依赖已移除 `markdown-it-katex`，high 项来自测试工具链 |

当前代码和文档以 Git 最新提交为准。三 IDE 的真实安装和交互验收不由构建命令代替，详见 [测试与验收清单](../05-测试与验收/测试与验收清单.md)。

## 目录结构

~~~text
README.md                         仓库入口
README-LOCAL-PATCH.md             完整增强版操作说明
CHANGELOG.md                      历史变更
src/                              扩展源码
resources/                         Webview 样式和资源
scripts/                           仓库级验证脚本
themes/markdown-theme/             独立 Less 主题工程
docs/                              分域文档与决策记录
release/                            本地 VSIX 输出目录，默认不进 Git
~~~

主题工程内部的文件职责见 [统一 Markdown 主题架构](../02-架构文档/统一Markdown主题架构.md)。

## 安装依赖

在仓库根目录执行：

~~~zsh
npm ci --no-audit --no-fund
npm --prefix themes/markdown-theme ci --no-audit --no-fund
~~~

如果需要固定 npm 缓存，可由本机自行增加 --cache 参数；缓存目录不属于仓库内容，也不应写入文档链接。

## 构建与打包

~~~zsh
npm run theme:build
npm run compile
npm run verify:security
npm run verify:local-patch
npm run verify:theme-system
npm run verify:docs
npx --yes @vscode/vsce@3.9.2 package --out "release/muhammad-ahmad.xlsx-viewer-1.9.98-local.3.vsix"
~~~

日常主题开发可用：

~~~zsh
npm run theme:watch
~~~

按 Ctrl + C 停止监听。构建失败时保留上一次成功 CSS 的行为由运行时加载器负责；主题构建器本身会以非零状态退出。

## 开发约束

- Less 只在 themes/markdown-theme 构建阶段编译，不在 Webview 运行时编译。
- 生成的 dist/markdown-theme.css 和 theme-manifest.json 由命令生成，不直接手改。
- 新增设置必须同时更新 package.json、运行时读取逻辑、验证脚本和文档。
- Markdown 主题选择器必须限定在预览或目录容器，不能覆盖 html、body 和工具栏。
- XLSX、CSV、TSV 代码不因 Markdown 主题需求顺手重构。

## 升级流程

1. 记录当前 Git 提交和 VSIX 文件，保留可回退基线。
2. 更新上游代码或依赖后先运行类型检查、Lint、主题构建和静态验证。
3. 公开发布前执行 `npm audit --audit-level=high --package-lock-only`，不得使用未经回归验证的 `npm audit fix --force`。
4. 检查 Markdown DOM、主题注入、配置变更和文件监听。
5. 打包新的本地 VSIX，并按测试清单做三 IDE 与 XLSX、CSV、TSV 人工回归。
6. 真实验收通过后再提交和推送；若失败，优先回退 VSIX 或 Git 提交，不修改用户 Markdown 正文。
